# Tree Sitter Bifrost Grammar

## Building
To build the Bifrost grammar for Tree Sitter, ensure you have the necessary build tools installed. Then, run the following command in the terminal:

```bash
pnpm install -g tree-sitter-cli
```
