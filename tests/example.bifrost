let io = import("std:io")

let test2 = struct {
    let value: str,
    let greet = [io.printf, this]() => null {
        io.printf("Hello %s\n", this().value)
    }
}

let main = [test2.greet]() => null {
    test2.greet()
}
